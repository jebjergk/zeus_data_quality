# UI Change Guidance

The Zeus Data Quality interface follows a documented UI Contract. Navigation,
page layouts, and configuration workflows are tightly coupled to downstream
processes and walkthrough guides.

## When in doubt, ask before changing UI/flows
If you are unsure whether a tweak might alter the expected user journey, confirm
with the product/design stakeholders or the feature owner before implementing
it. Early alignment prevents breaking downstream runbooks and internal demos.
